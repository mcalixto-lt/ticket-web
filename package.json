{
  "name": "ticket-web",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "description": "Ticket. — painel web mobile-first com câmera automática, seletor de horário, navegação interna, marca d’água e Google Drive.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@azure/msal-browser": "5.17.0"
  },
  "devDependencies": {
    "vite": "8.1.4"
  }
}
